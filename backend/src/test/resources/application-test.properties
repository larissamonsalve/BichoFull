# Habilita o Flyway para criar as tabelas no banco de teste do Docker
spring.flyway.enabled=true

# Impede que o Hibernate tente criar as tabelas (o Flyway já faz isso)
spring.jpa.hibernate.ddl-auto=validate

# Propriedade do JWT (Obrigatória para o TokenService não quebrar o contexto)
api.security.token.secret=BichoFullSecretKeyMuitoSeguraComMaisDe32Caracteres2026!